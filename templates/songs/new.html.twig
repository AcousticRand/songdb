{% extends "base.html.twig" %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% block body %}
        <div class="container">
                {{ form_start(form, {'action': path('song_create'), 'method': 'POST'}) }}
                {{ form_widget(form) }}
                <button type="submit" class="btn btn-primary btn-lg btn-block">{{ label }}</button>
                {{ form_end(form) }}
        </div>
{% endblock %}
